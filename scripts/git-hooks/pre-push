#!/bin/sh
# Run lint and build before pushing. Build includes the TypeScript typecheck.
# Skip with: git push --no-verify (not recommended).
set -e

echo "[pre-push] npm run lint"
npm run lint

echo "[pre-push] npm run build"
npm run build

echo "[pre-push] ok"
